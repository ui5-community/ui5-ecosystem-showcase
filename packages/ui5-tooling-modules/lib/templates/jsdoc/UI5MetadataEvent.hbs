/**
 * {{event.description}}
 */
"{{event.name}}": {
{{#if event.parameters}}
    "parameters": {
    {{#each event.parameters}}
    /**
     * {{{this.description}}}
     */
    "{{@key}}": {{{json this}}},
    {{/each}}
    }
{{/if}}
}