/**
 * {{event.description}}
 */
"{{event.name}}": {
{{#if event.parameters}}
    "parameters": {
    {{#each event.parameters}}
{{jsDoc @root.jsDoc "events" ../event.name @key}}
    "{{name}}": {{{json this}}},
    {{/each}}
    }
{{/if}}
}