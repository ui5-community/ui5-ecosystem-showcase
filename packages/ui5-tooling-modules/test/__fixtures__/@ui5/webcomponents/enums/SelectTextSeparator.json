{"name":"SelectTextSeparator","_ui5QualifiedName":"@ui5.webcomponents.SelectTextSeparator","_ui5QualifiedNameSlashes":"@ui5/webcomponents.SelectTextSeparator","description":"Defines the separator types for Select component two-column layout.","values":[{"name":"Bullet","description":"Will show bullet(·) as separator on two columns layout when Select is in read-only mode.","_jsDoc":"/**\n * Will show bullet(·) as separator on two columns layout when Select is in read-only mode.\n * \n * @private\n */"},{"name":"Dash","description":"Will show N-dash(–) as separator on two columns layout when Select is in read-only mode.","_jsDoc":"/**\n * Will show N-dash(–) as separator on two columns layout when Select is in read-only mode.\n * \n * @private\n */"},{"name":"VerticalLine","description":"Will show vertical line(|) as separator on two columns layout when Select is in read-only mode.","_jsDoc":"/**\n * Will show vertical line(|) as separator on two columns layout when Select is in read-only mode.\n * \n * @private\n */"}],"_jsDoc":"/**\n * Defines the separator types for Select component two-column layout.\n * \n * @enum {string}\n * @alias module:@ui5/webcomponents.SelectTextSeparator\n * @ui5-module-override @ui5/webcomponents SelectTextSeparator\n * @private\n */"}